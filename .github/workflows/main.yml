name: Notify reviewers by label

on:
  pull_request:
    types: [labeled, opened, synchronize]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Notify specific users if label matches
        uses: actions/github-script@v7
        with:
          script: |
            const labelToUsers = {
              "decap-cms/ready": ["pierretre"]
            };

            const pr = context.payload.pull_request;
            const labels = pr.labels.map(l => l.name);

            const usersToNotify = new Set();

            for (const label of labels) {
              if (labelToUsers[label]) {
                labelToUsers[label].forEach(u => usersToNotify.add(u));
              }
            }

            if (usersToNotify.size > 0) {
              const mentions = [...usersToNotify].map(u => `@${u}`).join(" ");
              await github.rest.issues.createComment({
                ...context.repo,
                issue_number: pr.number,
                body: `${mentions} — this change is ready for publishing.`
              });
            }
